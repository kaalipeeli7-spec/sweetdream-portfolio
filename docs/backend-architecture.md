# Backend Architecture

Backend built using:
- Node.js
- Express.js
- SQLite (better-sqlite3)
- Render.com hosting

Key modules:
- /events (event ingestion)
- /commands (remote command delivery)
- /upload (file uploads)
