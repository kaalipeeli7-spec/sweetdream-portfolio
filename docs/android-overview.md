# Android App Overview

The SweetDream Android Client collects device data, uploads events, listens for remote commands, and performs actions such as camera capture, audio monitoring, storage scans, call logs, notifications, and accessibility actions.
